import React, { useState } from 'react';

const Footer = () => {
  return (
    <footer>
      <button>Prev</button>
      <button>Skip</button>
      <button>Next</button>
      <button>Save</button>
      <button>Submit</button>
    </footer>
  );
};

export default Footer;
