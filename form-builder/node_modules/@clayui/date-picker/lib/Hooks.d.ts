import { Month } from './Helpers';
import { FirstDayOfWeek } from './types';
declare const useWeeks: (currentMonth: Date, firstDayOfWeek: FirstDayOfWeek) => [Month, (value: Date) => void];
declare const useCurrentTime: (format: string) => [string, (hours: number | string, minutes: number | string) => void];
export { useCurrentTime, useWeeks };
